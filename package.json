{
  "name": "agent-diff",
  "version": "0.1.0",
  "private": true,
  "description": "Review local code changes and discuss inline comments with an LLM in tmux.",
  "type": "commonjs",
  "bin": {
    "agent-diff": "server.js"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
